import React from "react";
import MultiStepAuthForm from "../forms/authentication/AuthStepWrapper";

const RegisterModal = () => {
  return (
    <div>
      <MultiStepAuthForm />
    </div>
  );
};

export default RegisterModal;
